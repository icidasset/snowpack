
export let foobar = 0;
if (process.env.NODE_ENV === 'production') {
  foobar = 1;
} 
if (process.env.NODE_ENV === 'development') {
  foobar = 2;
} 
if (process.env.NODE_ENV === 'some-env') {
  foobar = 3;
} 